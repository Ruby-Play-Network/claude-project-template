// Barrel of every table in the app. Each feature defines its own `*.table.ts`
// and re-exports it here so drizzle-kit sees the whole schema in one place.
//
// export * from '../../features/users/users.table.js';

export {};
